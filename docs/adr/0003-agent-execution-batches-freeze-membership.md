---
status: accepted
---

# Agent 执行批次冻结成员并与下一批候选分离

Editor 在提交反馈时持久化一个成员不可变的执行批次，当前批次运行期间新增的反馈任务只进入下一批候选；未完成任务保留原批次归属，必须由用户明确选择单独重试或合并到下一批。这个选择放弃了把全部 `pending` 任务自动汇总提交的便利，换取终端回合、任务面板和恢复流程对同一批次范围的一致理解，避免 steer 输入框、页面刷新或并发标注把新任务误注入正在执行的 Agent 回合。
